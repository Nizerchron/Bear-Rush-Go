package com.bearrushbuilder.app.ui.theme

import androidx.compose.ui.graphics.Color

val Primary = Color(0xFFFF9800)      // Honey Orange — aksi utama
val PrimaryDark = Color(0xFFF57C00)  // Deep Honey — hover/aktif
val Accent = Color(0xFFFFC107)       // Golden Yellow — koin & badge

// Light Theme
val BackgroundLight = Color(0xFFFFFBF0)
val SurfaceLight = Color(0xFFFFF3E0)
val OnPrimaryLight = Color(0xFFFFFFFF)
val OnBackgroundLight = Color(0xFF3E2723) // Deep chocolate brown — kontras tinggi
val OnSurfaceLight = Color(0xFF3E2723)

// Dark Theme
val BackgroundDark = Color(0xFF222222)
val SurfaceDark = Color(0xFF333333)
val OnPrimaryDark = Color(0xFFFFFFFF)
val OnBackgroundDark = Color(0xFFFFFFFF)
val OnSurfaceDark = Color(0xFFFFFFFF)

// Warna semantik — pakai ini di seluruh app, bukan Color(0xFF...)
val ColorSuccess = Color(0xFF4CAF50)   // Hijau sukses / preset gratis
val ColorLove = Color(0xFFE91E63)      // Merah muda — ikon love/favorit
val ColorPink = Color(0xFFF06292)      // Pink — badge "more"
val ColorDevMode = Color(0xFF0288D1)     // Biru terang - banner Developer Mode (legacy, kept for ref)
val ColorDevModeDark = Color(0xFF1A237E) // Navy gelap - gradient banner Developer Mode (legacy)
val ColorDevCard = Color(0xFF2C1B0E)     // Deep roasted cocoa - gradient start tombol dev
val ColorDevCardEnd = Color(0xFF7B3C10)  // Charred amber - gradient end tombol dev
val ColorButtonGreen = Color(0xFF00E676) // Hijau terang — tombol CTA sekunder
val ColorTextMuted = Color(0xFF7A8B99)  // Abu biru — teks sekunder kartu
val ColorCoinBadge = Color(0xFFFFD54F)  // Kuning emas — ikon koin kecil

